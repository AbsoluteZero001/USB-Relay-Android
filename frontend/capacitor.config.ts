import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.absolutezero.usbrelay',
  appName: 'USB Relay',
  webDir: 'dist-electron/renderer'
};

export default config;
